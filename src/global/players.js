import { playersHealth } from "./healthDefaults";

/**
 * Required user creation input fields.
 */
export const requiredFields = ["name"];

/**
 * Required default field values.
 */
export const defaultFields = { name: "", character: "", health: "" };

/**
 * Default values will vary depending on the difficulty.
 */
export { playersHealth };
